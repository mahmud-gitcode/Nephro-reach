import type { DialysisClinic } from "./clinic.types";

/** Demo content, deletable in one commit when real accounts arrive. */
export const SEED_CLINIC: DialysisClinic = {
  name: "ABC Dialysis Center",
  phone: "(305) 555-0142",
};
